
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const GermanDeficitCalculator = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Germany Deficit Calculator</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This tool is under construction. Advanced features for calculating Germany's budget deficit will be available here soon.</p>
      </CardContent>
    </Card>
  );
};

export default GermanDeficitCalculator;
