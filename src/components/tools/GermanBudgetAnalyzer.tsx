
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const GermanBudgetAnalyzer = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Germany Budget Analyzer</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This tool is under construction. Advanced features for analyzing the German budget will be available here soon.</p>
      </CardContent>
    </Card>
  );
};

export default GermanBudgetAnalyzer;
