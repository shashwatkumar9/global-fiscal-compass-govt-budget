
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const GermanBudgetAllocationTool = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Germany Budget Allocation Tool</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This tool is under construction. Advanced features for simulating budget allocation will be available here soon.</p>
      </CardContent>
    </Card>
  );
};

export default GermanBudgetAllocationTool;
