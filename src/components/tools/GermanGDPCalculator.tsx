
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const GermanGDPCalculator = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Germany GDP Calculator</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This tool is under construction. Advanced features for calculating Germany's GDP will be available here soon.</p>
      </CardContent>
    </Card>
  );
};

export default GermanGDPCalculator;
